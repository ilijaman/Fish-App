<%- include('./partials/head.ejs')%>

<%- include('./partials/flash.ejs')%>
    <h1>Fish</h1>

    <% for (let index=0; index < bookmarks.length; index++ ) { %>
        <p>
            <%= fish[index].name %>
                <form action="/fish/<%= fish[index]._id %>/delete" method="get">
                    <input type="submit" value="Delete" />
                </form>
        </p>
        <p>
            <a href="/fish/<%= fish[index]._id %>/edit">edit</a>
        </p>
        <a href="/fish/<%=fish[index]._id%>">View <%=`${fish[index].name}`%></a>
        <% } %>




            <a href="/fish/0/First"></a>